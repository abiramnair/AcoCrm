version: '3.8'

services:
  db:
    image: "postgres:13"
    environment:
      - "POSTGRES_HOST_AUTH_METHOD=trust"
    volumes:
      - postgres_data:/var/lib/postgresql/data/
  web:
    build: .
    environment:
      - "EMAIL_HOST_PASSWORD=kniayvlmmwpaifam"
      - "STRIPE_ENDPOINT_SECRET=whsec_XZF9tPSvjiIejHDsALvvq6Qzh8m0cp94"
    volumes:
      - .:/code
    ports:
      - 8000:8000
    depends_on:
      - db

volumes:
  postgres_data: